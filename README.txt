You can run project in your cmd:

gradle run --args "your path"

example:

gradle run --args "C:\testowy\creditsuisse\src\paths\test.txt"

You must have gradle and java8, added to your environmental variables.

You can first build project:

gradle build.

